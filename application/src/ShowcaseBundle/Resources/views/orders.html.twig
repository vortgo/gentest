{% extends  '@Showcase/layout.html.twig' %}

{% block title %}Showcase{% endblock %}

{% block body %}
    <div class="row">
        {% for order in orders %}
            <div class="col-md-4">
                {{ order.id }}
            </div>
        {% endfor %}
    </div>
{% endblock %}